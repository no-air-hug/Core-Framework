{%- liquid
  layout none
  # Shopper app utils
  if settings.shopper_enabled
    capture shopper_raw
      section 'shopper-settings'
    endcapture
    echo shopper_raw | split: '<script>' | last | split: '</script>' | first
  endif
-%}
